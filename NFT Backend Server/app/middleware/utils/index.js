const { removeExtensionFromFile } = require('./removeExtensionFromFile')
const { handleError } = require('./handleError')
const { buildErrorMessage } = require('./buildErrorMessage')

module.exports = { removeExtensionFromFile, handleError, buildErrorMessage }
